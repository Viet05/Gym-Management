spring:
  config:
    import: optional:file:.env

  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3307}/${DB_NAME:GymDB}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  jackson:
    time-zone: Asia/Ho_Chi_Minh

server:
  port: ${SERVER_PORT:8080}

jwt:
  secret: ${JWT_SECRET:KJ5V7oXO1ddz_utM0Y1ATqa9klyETrc6z9SRjrOB87k}

vnpay:
  url: ${VNPAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
  return-url: ${VNPAY_RETURN_URL:http://localhost/api/payment/vnpay/return}
  ipn-url: ${VNPAY_IPN_URL:http://localhost/ipn}
  tmn-code: ${VNPAY_TMN_CODE:99M6VKL1}
  hash-secret: ${VNPAY_HASH_SECRET:HXT6A4JBHBEZVGZMZSCYQI3FSO8M1557}
  version: ${VNPAY_VERSION:2.1.0}
  command: ${VNPAY_COMMAND:pay}
  curr-code: ${VNPAY_CURR_CODE:VND}
  locale: ${VNPAY_LOCALE:vn}
  expire-minutes: ${VNPAY_EXPIRE_MINUTES:15}
  time-zone: ${VNPAY_TIME_ZONE:Asia/Ho_Chi_Minh}
